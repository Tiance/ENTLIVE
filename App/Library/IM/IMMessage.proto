syntax = "proto3";

message IMMessage {

    enum CMD {
        GIFT = 0;
    }

    enum CMDTYPE {
        GIFT_SEND = 0;
    }

    int32 cmdType = 1;
    int32 cmd = 2; // Unique ID number for this person.
}